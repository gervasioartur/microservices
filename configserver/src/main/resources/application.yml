server:
  port: 8071
spring:
  application:
    name: 'configserver'
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: 'classpath:/config'
#          search-locations: 'classpath:/config' we can use also file system path
